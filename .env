PORT=4000

DB_USER=SAN
DB_PASSWORD=ing
DB_HOST=localhost
DB_PORT=5432
DB_NAME=ephemeral
SECRET_KEY=@mrKing

POSTING_BYPASS_ENABLE=true
# Optional: restrict bypass to certain user IDs (BIGINT). Leave empty to allow all in dev.
POSTING_BYPASS_USER_IDS=

# Alternatively (dev only), you can send a header to bypass from the client:
#   x-ephemeral-bypass-posting: true
# The header is ignored in production (NODE_ENV=production).
NODE_ENV=development